// window.onscroll = function() {myFunction()};

// 					var header = document.getElementById("myHeader");
// 					var sticky = header.offsetTop;

// 					function myFunction() {
// 					  if (window.pageYOffset > sticky) {
// 					    header.classList.add("sticky");
// 					  } else {
// 					    header.classList.remove("sticky");
// 					  }
// 					}


//Get the div that will be clicked on

//add a click event handler onto the div (above)

//when the event handler is called (is clicked), then call a function (add new image)

//add a global variable that holds the image size

//declare a function:
	//inside that add a version of the image to the div
	//get the value of the image size variable (set it as the size of the new image using CSS)
	//change the value of the global variable to 10% of the previous size
	